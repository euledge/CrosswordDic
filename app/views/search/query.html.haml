%h1= "#{@query_word} の 検索結果"

= @redis_instance
%table
  - @words.each do |word|
    %tr
      %th 読み:
      %td= word['yomi']
      %th 単語:
      %td
        = link_to(word['word'], 'http://dic.yahoo.co.jp/dsearch?dtype=0&p=' + word['word'])
